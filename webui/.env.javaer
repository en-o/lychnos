# jar合并部署模式配置示例 - 这是一个配置示例文件
# 使用方法: cp .env.mock .env 或者在命令行中通过 cross-env 传入参数`VITE_USE_MOCK=true`
# API Base URL
# jar合并部署
VITE_API_BASE_URL=/

# 是否使用Mock数据 (true: 使用mock数据, false: 使用真实接口)
VITE_USE_MOCK=false

# 前端访问路由前缀 (例如: /lychnos, 留空则为根路径)
VITE_BASE_PATH=/
